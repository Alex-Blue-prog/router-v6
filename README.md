# Page with react v6 routers

## Description
galery of photos, the photos are comming from a jsonplaceholder api, the requests were made by the axios library.

commands to run the application:

- `npm install`

- `npm start`